<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%=Helpers.current_user(@session).username%>
    <%if Helpers.is_logged_in?(@session) == true%>
    <%=Helpers.current_user(@session).balance%>
  </body>
</html>
